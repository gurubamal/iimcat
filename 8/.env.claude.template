# Claude AI Provider Configuration Template
# Copy this to .env and fill in your values
# Usage: cp .env.claude.template .env && nano .env
# Then: set -a; source .env; set +a

# ============================================
# CLAUDE (Anthropic) Configuration
# ============================================

# Required: Your Claude API key from https://console.anthropic.com/account/keys
ANTHROPIC_API_KEY=sk-ant-api03-xxxxx-your-key-here-xxxxx

# Optional: Model selection (default: claude-3-5-sonnet-20240620)
# Options:
#   - claude-3-5-sonnet-20240620  (balanced, recommended)
#   - claude-3-opus-20240229      (most capable, slower, expensive)
#   - claude-3-haiku-20240307     (fastest, cheapest)
#ANTHROPIC_MODEL=claude-3-5-sonnet-20240620

# Optional: Temperature (0.0-1.0, default: 0.2)
# Lower = more conservative, Higher = more creative
#ANTHROPIC_TEMPERATURE=0.2

# Optional: Max response tokens (default: 1200)
#ANTHROPIC_MAX_TOKENS=1200

# Optional: Request timeout in seconds (default: 90)
#ANTHROPIC_TIMEOUT=90

# Optional: API version (default: 2023-06-01)
#ANTHROPIC_VERSION=2023-06-01

# Optional: Custom system prompt
#ANTHROPIC_SYSTEM_PROMPT="You are an expert Indian equity analyst focusing on NSE stocks."

# ============================================
# OpenAI (Codex/GPT) Configuration (Optional)
# ============================================

# If you want to use OpenAI alongside Claude
#OPENAI_API_KEY=sk-xxxxx-your-openai-key-here
#OPENAI_MODEL=gpt-4.1-mini
#OPENAI_TEMPERATURE=0.2

# ============================================
# Provider Preferences
# ============================================

# Default provider when using auto-detection
# Options: claude, codex, heuristic, auto
# If not set, auto-detection prioritizes: Claude > Codex > Heuristic
AI_PROVIDER_DEFAULT=claude

# Or force a specific provider for all operations
#AI_PROVIDER=claude

# ============================================
# Shell Bridge Configuration (Fallback)
# ============================================

# Only needed if not using direct API access
#CODEX_SHELL_CMD=python3 codex_bridge.py
#CURSOR_SHELL_CMD=python3 cursor_ai_bridge.py
#AI_SHELL_CMD=python3 codex_bridge.py

# ============================================
# Analysis Configuration
# ============================================

# Ticker file to use
TICKERS_FILE=all.txt

# Hours to look back for news
HOURS_BACK=48

# Maximum articles per ticker
MAX_ARTICLES=10

# Maximum AI calls per run (budget control)
#MAX_AI_CALLS=100

# Output file
OUTPUT_FILE=realtime_ai_rankings.csv

# ============================================
# Advanced Options
# ============================================

# Verify internet connectivity before running
#VERIFY_INTERNET=1

# Require internet for AI provider (fail if offline)
#REQUIRE_INTERNET_AI=1

# Probe agent internet access
#PROBE_AGENT=1

# Require agent internet access
#REQUIRE_AGENT_INTERNET=1

# ============================================
# Notes
# ============================================
#
# 1. Never commit .env to git! Add it to .gitignore
# 2. Get Claude API key: https://console.anthropic.com/account/keys
# 3. Test setup: ./check_claude_setup.sh
# 4. Quick start: ./run_with_claude.sh
# 5. Full scan: ./optimal_scan_config.sh (auto-detects Claude)
#
# Cost reference (as of Oct 2024):
#   Claude Sonnet 3.5: $3/MTok input, $15/MTok output
#   Claude Opus: $15/MTok input, $75/MTok output
#   Claude Haiku: $0.25/MTok input, $1.25/MTok output
#   GPT-4-mini: $0.15/MTok input, $0.60/MTok output
#
# Typical usage:
#   1000 articles × 1200 tokens = ~1.2M tokens
#   Claude Sonnet cost: ~$3.60-18.00
#   GPT-4-mini cost: ~$0.18-0.72
